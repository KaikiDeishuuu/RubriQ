This directory contains the FastAPI backend for QuizOCR Grader.

Run the backend from the repository root using Docker Compose or point uvicorn at `app.main:app` from this directory after installing backend dependencies.
