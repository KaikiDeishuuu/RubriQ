Extract the student metadata and answers from the attached pages.

Exam title: ${exam_title}
Questions reference:
${questions_json}

Pages are attached in order.

Preserve formulas exactly as written in the images. Do not add, remove, simplify, or infer terms. Pay special attention to exponents, multiplication signs, denominators, units, parentheses, and handwritten formula structure. If OCR reference text conflicts with the image, trust the image. If you are unsure about a formula or answer, keep the text conservative and set lower confidence instead of guessing.

${ocr_reference_text}

Return JSON only.
